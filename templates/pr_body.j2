## Automated firewall change proposal — {{ request.number | default('CHGXXXX') }}

**AI traces the path. Human approves. AAP executes.** This PR was opened by the
firewall change pipeline (JT1). It is **not** merged automatically — an engineer
approves it (Gate 1), CAB approves the change (Gate 2), and AAP merges it to
`{{ git_main_branch | default('main') }}` **only after** the firewall change is
applied and validated.

### Request
| Field | Value |
|-------|-------|
| Source | `{{ request.source_ip }}` |
| Destination | `{{ request.destination_ip }}` |
| Protocol / port | `{{ request.protocol }}` / `{{ request.port }}` |
| Requested by | {{ request.requester | default('unknown') }} |
| ServiceNow | {{ request.number | default('n/a') }} |

### AI path trace
> The AI traced the traffic path across firewalls to determine which devices
> need rules and what zone pairs to use. **Review the trace below**

{{ path_trace | default('(no path trace provided)') }}

### Authored rules
> Rules below were authored by the AI based on the path trace. The reviewer should
> verify that each zone pair matches the traced path and that the application/service
> match the requested protocol/port.

{% for r in authored_rules %}
**{{ loop.index }}. `{{ r.rule_name }}`** on `{{ r.target_firewall }}`
| Field | Value |
|-------|-------|
| Direction | `{{ r.source_zone | join(',') }}` → `{{ r.destination_zone | join(',') }}` |
| Source | `{{ r.source_ip | join(',') }}` |
| Destination | `{{ r.destination_ip | join(',') }}` |
| Application | `{{ r.application | join(',') }}` |
| Service | `{{ r.service | join(',') }}` |
| Action | `{{ r.action }}` |
| Placement | `{{ r.location | default('before') }}` `{{ r.existing_rule | default('DENY-ANY-ANY') }}` |
| Description | {{ r.description | default('n/a') }} |

{% endfor %}

### Change rationale
{{ analysis | default('See ServiceNow change request.') }}

---
🤖 Generated by the AI firewall change pipeline. Source of truth: `{{ git_main_branch | default('main') }}` (written only on successful deploy).
